
from . import Task


class CheckForEmptyMotives(Task):
    """
    Check for users that have not created any motives and send them an introductory email.
    make sure now to spam the user, so log that the email was sent and only send it
    every month?
    """
    pass
